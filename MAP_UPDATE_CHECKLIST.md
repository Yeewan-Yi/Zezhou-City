# 地图更新检查清单

收到新版 `泽州non.svg` 后，按下列范围统一更新，除非任务明确说明仅更新指定板块。

公交线路页面的目录、站点信息、环线／非环线布局、换乘与交互规范另见 `BUS_DESIGN_REQUIREMENTS.md`。

## 基础文件

- 将新版文件同步至 `assets/maps/zezhou-base.svg`。
- 将公交等仍引用版本化底图的文件同步更新；当前包括 `assets/maps/zezhou-base-20260730.svg`。
- 保持网页展示框的宽高、纵横比和缩放方式不变。
- 记录新版 SVG 的 `viewBox`。若内部坐标范围发生变化，必须同步校正嵌入式 SVG 的 `x`、`y`、`width`、`height`，不得只替换文件。

## 网页检查范围

- 城市地图：完整现状地图、行政区划总图及各区局部图。
- 公共交通：301 路、401 路及后续新增线路的局部底图。
- 城市设施：所有由基础地图裁剪生成的设施位置图。
- 其他直接或间接引用 `assets/maps/` 基础地图的页面。

## 验收

- 全局搜索旧地图文件名与版本号，确认没有遗漏引用。
- 检查完整地图的显示范围、比例与原版一致。
- 检查公交站点及轨迹仍与新版道路对齐。
- 检查设施位置图以目标设施为视觉中心，周边道路和地名清晰可辨。
- 在桌面和窄屏尺寸下检查布局、交互及控制台错误。

## 最新基准数据（2026-08-04）

- 最新来源：`D:\桌面\sky\zzmap\泽州non.svg`。
- 基础地图尺寸：`1024 × 1024 px`；内部 `viewBox`：`1820 1820 4551 4551`。
- 最新基础地图 SHA-256：`9B092AAD5EB23A91E27BB12521B05028CB1515CB5DF0BA0464E4B8B91B199854`。
- 当前基础文件：`assets/maps/zezhou-base.svg`、`assets/maps/zezhou-base-20260730.svg`，两者内容与最新来源一致。
- 301 路底图嵌入范围：`x=1820`、`y=1820`、`width=4551`、`height=4551`；局部图 `viewBox`：`5611.96 1893.02 759.04 983.03`。该范围保持原纵向尺度，并将线路主体右移，避免新版底图右侧边界之外出现空白。透明悬停层必须按 `translate(910 910) scale(6.2216796875)` 后的实际坐标换算，确保与可见站点圆圈重合。
- 401 路局部图 `viewBox`：`640 250 330 290`；去返程轨迹各 9 段，站点 10 个。
- 333 路为三山区东郊街道环线，局部图 `viewBox`：`5680 1825 760 1040`；外环按逆时针、内环按顺时针运行，使用 `333（外环）`、`333（内环）`作为方向名称。线路轨迹取自 `D:\桌面\sky\zzmap\333路.svg` 的 14 段道路走向，站点 14 个；东郊地铁站、滨郊南路与 301 路为同一站，两个页面均须显示301／333互相换乘。方向切换按钮固定显示“东郊地铁站—东郊地铁站”，主线路标题随方向切换；左侧站点图使用居中收窄的圆角矩形，站名置于环线外侧、四个转角不设站，四边以无底色箭头统一标示顺时针方向，方向徽标使用圆形“外／内”。
- 泽州市第一中心学校坐标：`3750.95, 3677.03`；周边图 `viewBox`：`3150.95 3077.03 1200 1200`。该图必须在详情页内嵌 SVG 中直接引用 `assets/maps/zezhou-base.svg`，不得通过外层 `<img>` 加载再次引用底图的 SVG 文件，否则线上浏览器可能阻止嵌套资源而显示空白。
