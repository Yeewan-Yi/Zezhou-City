# 泽州市地图源文件分析

## 当前结论

两份 SVG 使用同一坐标范围 `910 910 6371 6371`，画面尺寸均为
`1024 × 1024`。文件本身已经按照地图要素分组，可以作为后续网页地图的
原始数据来源。

- `zezhou-base.svg`：来自“泽州non.svg”，不包含铁路或地铁路径，适合作为基础底图。
- `zezhou-transport-full.svg`：来自“泽州.svg”，保留原有轨道交通路径，适合核对交通叠加关系。

处理时移除了旧版 SVG DOCTYPE、压缩标签间空白，并将过长的小数坐标统一到
两位精度；没有合并或删除地图路径。

## 已识别图层

| SVG 分组 | 内容 | 数量 | 后续用途 |
| --- | --- | ---: | --- |
| `group_terrain` | 地形与地表 | 869 条路径 | 基础底色、绿地和地貌 |
| `group_water` | 河流与水域 | 20 条路径 | 水系图层 |
| `group_building` | 建筑及设施轮廓 | 121 条路径 | 城市设施与地标 |
| `group_road` | 道路几何 | 16019 条路径 | 道路网络 |
| `group_railway` | 轨道几何 | 完整版 5 条、底图版 0 条 | 铁路、地铁叠加 |
| `group_mapsymbol` | 地图符号 | 当前为空 | 后续放置站点和公共设施图标 |
| `group_text` | 道路、设施、行政区文字 | 底图版 814 个 | 地图标注 |

文字采用“白色描边文字 + 正常文字”的双层写法，所以 812 个 `<text>` 并不
代表 812 个不同地点。底图中识别到 8 个行政区标签、73 个设施标签和 325 个
道路标签（均未扣除双层重复）。

## 现有数据的限制

1. 道路由 16019 条绘制路径组成，而不是一条道路对应一个带名称的对象。
2. 行政区目前只有文字标签，没有独立、闭合且可点击的区界多边形。
3. 建筑名称与建筑轮廓之间没有唯一 ID 关联。
4. 坐标为游戏地图局部坐标，不是真实经纬度；用于架空城市没有问题，但导入
   GIS 时需要建立统一的“泽州本地坐标系”或映射到一块虚拟经纬度范围。
5. 完整版轨道交通只有少量合成路径，不能直接区分每条线路和每座车站。

## 下一阶段的数据拆分

建议不要直接把 16019 条道路路径逐条转换成 GeoJSON。先在 QGIS 中建立以下
人工校正图层：

1. `districts`：行政区多边形，字段包括 `name`、`name_en`、`seat`。
2. `roads_major`：快速路、主干路和重要桥隧中心线，字段包括 `name`、
   `class`、`bridge`、`tunnel`。
3. `rail_lines`：每条铁路和地铁线路，字段包括 `line_no`、`name`、`color`、
   `status`。
4. `stations`：铁路、地铁和公交枢纽点，字段包括 `name`、`name_en`、
   `lines`、`transfer`。
5. `places`：政府、医院、学校、公园、商业中心和地标。

网站第一阶段可以直接以 SVG 作底图，并为行政区、交通线和地点增加透明的
交互覆盖层；数据稳定后，再把上述图层输出为 GeoJSON，由 MapLibre 渲染。

## 已确认的文字修订

- 删除错误的“高 高地”行政区标签。
- 将“江州新城”统一修订为“江洲新城”。

## 需要人工确认的内容

- 行政区边界的准确走向。
- 地图朝向是否确认为上北下南。
- 是否为地图设置比例尺；如果设置，需要提供游戏中任意两点的实际距离。
