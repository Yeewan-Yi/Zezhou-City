# 公交板块设计要求

本文档用于统一泽州市门户“公共交通—公交”板块的线路目录、线路详情、局部地图和交互设计。新增或调整公交线路时，应同时遵循本文件与 `MAP_UPDATE_CHECKLIST.md`。

## 一、信息来源与基本原则

- 线路走向、站点位置和道路关系以用户提供的最新版城市地图及线路 SVG 为依据。
- 线路 SVG 用于提取道路走向和站点坐标，不直接作为网页展示图片。
- 局部线路图应使用网站统一底图重新绘制线路、站点、站名和交互层，不得使用截图代替。
- 线路轨迹必须拟合底图中的实际道路，不得用无意义的直线或平滑曲线连接站点。
- 页面面向市民和游客，标题、说明和公告应采用正式、清晰的公共服务措辞，不添加制作过程、占位说明或与政务网站无关的小字。

## 二、线路目录与路由

- 公交首页按编号范围展示线路分类。
- 打开编号范围后，首先展示该范围内各线路的链接列表，用户点击后进入具体线路详情页。
- 每条线路必须具有独立路由，格式为 `#transit-bus-线路号`。
- 目录项至少显示线路号、服务区域和起讫站；环线显示“首末站环线”。
- 当前已发布线路包括 301 路、333 路和 401 路。

## 三、线路详情页通用结构

- 页首显示公交线路号、服务区域、线路性质和方向切换按钮。
- 左侧展示当前方向的站点线路信息，右侧展示局部线路地图。
- 方向切换后，线路标题、站点顺序、方向名称和辅助状态必须同步更新。
- 切换动画应在当前区域内完成，不得出现站点圆圈从页面边界飞入等异常位移。
- 左侧站点信息仅标注地铁换乘，不显示公交换乘；公交换乘统一在地图站点弹窗中展示。
- 地铁换乘标识沿用“公共交通—地铁”板块的线路色码。单线显示“X号线”，同站多线仅显示数字，并保持线路色块总宽度固定。

## 四、非环线设计

- 非环线使用“去程／返程”方向，切换按钮显示对应起点和终点。
- 双向共同停靠站无需附加文字；单向停靠站必须注明开往方向。
- 左侧站点线路信息区域高度统一固定为 `580px`，以当前 401 路的展示长度为基准；站点数量较少时均匀分布，不得因线路站数不同造成详情页左栏长度忽长忽短。
- 站点圆圈、序号、站名、地铁换乘标识和方向性停靠说明应保持统一对齐。

## 五、环线设计

- 环线方向名称应明确，例如 `333（外环）`、`333（内环）`，主线路标题随方向切换。
- 方向切换按钮显示首末站相同的闭合关系，例如“东郊地铁站—东郊地铁站”。
- 左侧站点图采用居中、收窄的圆角矩形环线，不使用有明显头尾的纵向线路。
- 站名位于环线外侧，四个圆角转角处不设置站点。
- 四条边分别设置 `→、↓、←、↑` 无底色箭头，视觉方向统一为顺时针；箭头不得遮挡站点、站名或换乘标识。
- “外／内”方向徽标使用圆形样式，环线上的所有站点圆圈尺寸一致，不通过首尾圆圈大小暗示起终点。

## 六、局部线路地图

- 地图展示范围应覆盖完整线路并保留适当边距，不得出现大片无效留白。
- 去返程或内外环可以叠加显示，但必须通过颜色、虚实线或箭头清楚区分。
- 站点圆圈必须位于线路轨迹节点上，站名不得大面积遮挡道路或彼此重叠。
- 地图底图保持统一的灰度、饱和度和明度处理，公交线路与站点应具有足够对比度。
- 地图更新时必须保持既有裁剪范围、缩放比例和坐标换算；若基础 SVG 的 `viewBox` 变化，须同步校正所有公交地图。

## 七、站点换乘与弹窗

- 鼠标经过或键盘聚焦站点时自动显示站点信息，点击站点也应能够显示。
- 鼠标离开局部地图范围后，站点信息自动隐藏。
- 弹窗应浮现在对应站点旁，并根据站点所在位置自动选择左侧或右侧，避免越出地图边界。
- 弹窗显示本站可换乘的公交线路和地铁线路；公交使用深绿色圆形徽标，地铁使用对应线路色码的矩形徽标。
- 同一站点的多条地铁线路合并为一个地铁换乘组；公交线路分别显示圆形线路号。
- 方向性公交站仅在需要时注明开往方向，双向停靠不重复标注。
- 同名且明确为同一站的线路必须互相显示换乘。当前 301 路与 333 路的共同站为“东郊地铁站”和“滨郊南路”。
- 透明悬停按钮的圆心必须与可见站点圆圈重合，不能只保证点击后有效。

## 八、响应式与无障碍

- 桌面端采用左侧站点信息、右侧地图的双栏布局；窄屏改为单栏排列。
- 窄屏下不得出现横向滚动、站名裁切、换乘徽标越界或箭头遮挡。
- 所有交互站点使用按钮元素并提供明确的 `aria-label`；方向切换按钮应描述将要切换到的方向。
- SVG 应包含标题和说明，站点弹窗使用适当的实时区域提示更新。

## 九、验收与发布

- 检查线路数据、站点顺序、方向名称及同站换乘信息。
- 检查每个站点圆心与线路轨迹节点的坐标误差，并检查透明交互层与可见圆圈的屏幕位置偏差。
- 分别检查桌面和窄屏布局、方向切换动画、鼠标悬停、点击、键盘聚焦及离开地图后的隐藏行为。
- 检查控制台错误、无效文本和 `undefined` 字样。
- 完成后更新资源版本号、提交并推送至 GitHub，确认 GitHub Pages 构建和部署成功。
